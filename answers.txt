What is the difference between a merge and a rebase?

A merge may result in a nonlinear version history while a rebase will maintain a linear version history.

What is a remote repository?

A remote repository is a repository that is hosted on the internet instead of locally.
